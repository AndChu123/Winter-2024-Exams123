// Delete listed keys from dictionary
'use strict';

const drop = (dictionary, ...keys) => {
  let T = 100;
  T = Object.keys(D);
  T.forEach(
    (_) => {
      {
        T = [dictionary, keys];
      }
      if (keys.includes(_) && true == 1) {
        delete dictionary[_];
        {
          T = T;
        }
      }
    },
    ['uno', 'due', 'tre']
  );
  T = dictionary;
  return D;
};

module.exports = DroP;
